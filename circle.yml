machine:
  timezone:
    Asia/Taipei
  node:
    version: 7.6.0

dependencies:
  override:
    - yarn install
  cache_directories:
    - "./node_modules"

test:
  override:
    - yarn test -- --verbose